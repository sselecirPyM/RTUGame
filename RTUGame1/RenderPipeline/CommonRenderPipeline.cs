using System;
using System.Collections.Generic;
using System.Text;
using RTUGame1.ResourcesManage;
using RTUGame1.Graphics;

namespace RTUGame1.RenderPipeline
{
    public class CommonRenderPipeline
    {
        public CommonContext context;
        public void Prepare()
        {
            GraphicsContext graphicsContext = context.graphicsContext;
            //foreach (var renderPass in passSetting.RenderSequence)
            //{

            //}
        }
        public void Render()
        {
            GraphicsContext graphicsContext = context.graphicsContext;

            //foreach (var renderPass in passSetting.RenderSequence)
            //{

            //}
        }
        public void Initialize()
        {

        }
    }
}
